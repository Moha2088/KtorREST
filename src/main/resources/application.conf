ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}

jwt {
    secret = "mySecret123"
    issuer  = "MoCorp"
    audience = "Mohamed"
    realm = "com.example"
}
